import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.sanad.coach",
  appName: "سند",
  webDir: "out",
  android: {
    backgroundColor: "#f1f3f7",
  },
};

export default config;
